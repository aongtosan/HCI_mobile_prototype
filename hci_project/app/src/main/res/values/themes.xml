<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Hci_project" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Customize your theme here. -->
        <item name="android:windowAnimationStyle">@style/animation</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <item name="android:textAppearance">@font/tufont</item>
        <item name="android:textViewStyle">@style/TU_TextViewStyle</item>
        <item name="android:buttonStyle">@style/TU_ButtonStyle</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="animation">
        <item name="android:windowEnterAnimation">
            @android:anim/fade_in
        </item>
        <item name="android:windowExitAnimation">
            @android:anim/fade_out
        </item>
    </style>
    <style name="PopupMenu" parent="@android:style/Widget.PopupMenu">
        <item name="android:popupBackground">@drawable/menu_background</item>
        <item name="android:dropDownVerticalOffset">0dp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="TU_TextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/tufont</item>
    </style>

    <style name="TU_ButtonStyle" parent="android:Widget.Holo.Button">
        <item name="android:fontFamily">@font/tufont</item>
    </style>
</resources>